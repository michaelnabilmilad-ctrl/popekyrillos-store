# دليل إدارة المنتجات

افتح صفحة الإدارة من:

```text
admin.html
```

أو بعد النشر:

```text
https://popekyrillos.store/admin.html
```

## تعديل منتج موجود

1. اضغط **فتح ملف products.json** واختر ملف `products.json` من فولدر الموقع.
2. اختار المنتج من القائمة.
3. عدل الاسم، السعر، الحالة، الوصف، الصور أو الاختيارات.
4. اضغط **حفظ في نفس ملف products.json** لو المتصفح يسمح بالحفظ.
5. لو الحفظ المباشر غير متاح، اضغط **تحميل products.json** واستبدل الملف القديم بالملف الجديد.

## تعديل الكمية

داخل قسم **الاختيارات والأسعار والكميات**:

- فعل أو ألغِ **متاح للبيع**.
- اكتب الكمية في خانة **الكمية المتاحة**.
- لو المنتج له أكثر من مقاس أو لون، عدل الكمية لكل اختيار على حدة.

## إضافة صور

1. ضع الصورة داخل فولدر `assets` في المشروع.
2. اكتب مسار الصورة في خانة **مسارات الصور**، كل صورة في سطر.
3. أول صورة في القائمة هي صورة كارت المنتج.
4. لو اختيار معين له صور مختلفة، اكتب صوره داخل خانة **صور هذا الاختيار**.

مثال:

```text
assets/optimized/products/example.webp
assets/detail/products/example-large.webp
```

## إضافة منتج جديد

1. اضغط زر **+** في قائمة المنتجات.
2. اكتب اسم المنتج، القسم، التصنيف، السعر، الوصف والصور.
3. لو له اختيارات، اضغط **إضافة اختيار** واكتب السعر والكمية لكل اختيار.
4. احفظ أو صدّر ملف `products.json`.

## نشر التعديلات

بعد حفظ `products.json`:

1. ارفع الملف الجديد على GitHub مكان الملف القديم.
2. لو تستخدم Firebase Functions أو ملف `functions/products.json`، اضغط **تحميل functions-products.json** ثم غيّر اسم الملف إلى `products.json` وارفعه مكان الملف القديم داخل فولدر `functions`.
3. اعمل Commit للتغييرات.
4. افتح الموقع وجرب المنتج المعدل.

## ملاحظات مهمة

- صفحة الإدارة لا تغير الموقع المنشور مباشرة؛ هي تعدل أو تصدر ملف المنتجات.
- لا تحذف كود المنتج إلا لو عارف إن روابط المنتج القديمة مش مهمة.
- الصور الجديدة لازم تكون موجودة فعلاً داخل فولدر الموقع قبل نشر الملف.
