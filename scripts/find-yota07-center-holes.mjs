import sharp from"sharp";import path from"node:path";const root=path.resolve(import.meta.dirname,".."),o=await sharp(path.join(root,"coloring","yota-07","outline.png")).ensureAlpha().raw().toBuffer({resolveWithObject:true}),W=o.info.width,dark=(x,y)=>{let p=(y*W+x)*4,l=.2126*o.data[p]+.7152*o.data[p+1]+.0722*o.data[p+2];return o.data[p+3]&&l<220},x=530,y=790,w=180,h=160,seen=new Uint8Array(w*h),cs=[];for(let yy=0;yy<h;yy++)for(let xx=0;xx<w;xx++){let s=yy*w+xx;if(seen[s]||dark(x+xx,y+yy))continue;let q=[s],edge=false;seen[s]=1;for(let i=0;i<q.length;i++){let p=q[i],cx=p%w,cy=Math.floor(p/w);if(!cx||!cy||cx===w-1||cy===h-1)edge=true;for(const[nx,ny]of[[cx-1,cy],[cx+1,cy],[cx,cy-1],[cx,cy+1]])if(nx>=0&&ny>=0&&nx<w&&ny<h){let np=ny*w+nx;if(!seen[np]&&!dark(x+nx,y+ny)){seen[np]=1;q.push(np)}}}if(!edge&&q.length>10){let xs=q.map(p=>p%w+x),ys=q.map(p=>Math.floor(p/w)+y);cs.push({n:q.length,x:Math.min(...xs),y:Math.min(...ys),w:Math.max(...xs)-Math.min(...xs)+1,h:Math.max(...ys)-Math.min(...ys)+1,cx:xs.reduce((a,b)=>a+b,0)/q.length,cy:ys.reduce((a,b)=>a+b,0)/q.length})}}console.log(cs);
