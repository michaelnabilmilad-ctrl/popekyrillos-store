# تقرير تحسين الأداء

تاريخ القياس: 19 يوليو 2026. القياس باستخدام Lighthouse بوضع Mobile. القياس السابق على النسخة المنشورة، والقياس اللاحق على بناء Cloudflare المحلي بعد التعديلات.

| المؤشر | قبل | بعد | التحسن |
|---|---:|---:|---:|
| Performance | 60 | 86 | +26 نقطة |
| LCP | 4.21 ثانية | 3.32 ثانية | 21% أسرع |
| Total Blocking Time | 1,050 ms | 243 ms | 77% أقل |
| CLS | 0.105 | 0.000 | أزيل التحرك المرئي في القياس |
| البيانات المنقولة | 1.46 MB | 0.56 MB | 62% أقل |
| Network requests | 38 | 18 | 53% أقل |

## ما تم تنفيذه

- صفحة منتج مستقلة لا ترندر أو تحمل أقسام الصفحة الرئيسية.
- API كتالوج داخلي يعيد DTO خفيفًا وثابت الحقول، مع pagination وفلاتر وبحث وترتيب.
- تحميل 12 منتجًا في البداية و12 إضافية عند الضغط على «عرض المزيد».
- كاش عام للكتالوج لمدة 10 دقائق، مع تغيير نسخة الكاش بعد تحديث المنتجات من الأدمن.
- إبقاء Airtable داخل Cloudflare Worker والوظائف الخلفية فقط.
- thumbnails محلية WebP بعروض 320 و480 و640 لكل المنتجات الموجودة وقت البناء، مع `srcset` و`sizes` وlazy loading.
- تحويل الخط الحالي نفسه إلى WOFF2، مع `font-display: swap` وpreload واحد.
- تأجيل تحميل إعدادات Firebase وSDK الخاص بالحساب إلى أول فتح لتسجيل الدخول.
- رؤوس كاش منفصلة للصور والأصول العامة، ومنع الكاش للصفحات والواجهات الشخصية.

## ملاحظات متبقية

- Brotli وHTTP/3 وEarly Hints وCache Rules ميزات على مستوى لوحة Cloudflare، ويجب تفعيلها على الـ zone المنشور؛ الكود ورؤوس الاستجابة متوافقة معها.
- القياس اللاحق محلي، لذلك قد تختلف النتائج النهائية قليلًا بعد النشر تبعًا لإعدادات Cloudflare والمسافة إلى مركز البيانات.
- المنتجات الجديدة المضافة بعد آخر build تحتاج تشغيل build لتوليد thumbnails المحلية لها.
